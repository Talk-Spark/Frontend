// 카드 정보 받아와서 map 함수로 카드 컴포넌트 생성
import NameCardComponent from "./NameCardComponent";

const CardStorage = () => {
  return (
    <div>
      <NameCardComponent />
    </div>
  );
};

export default CardStorage;
